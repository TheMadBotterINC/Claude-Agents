interface:
  display_name: "UX Tester"
  short_description: "Exhaustive browser UX QA with screenshots"
  default_prompt: "Use $ux-tester to audit this running web app's UX across desktop, tablet, and mobile."
dependencies:
  tools:
    - type: "mcp"
      value: "playwright"
      description: "Playwright MCP browser tools for Chrome navigation, snapshots, screenshots, console logs, network requests, and viewport resizing."
